[project]
name = "aiogram-metrics"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.28.0",
    "prometheus-client>=0.20.0",
]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.format]
quote-style = "single"
